import type {Config} from 'tailwindcss';

// This file is intentionally left blank to avoid conflicts.
// The main configuration is in the root `tailwind.config.ts`.

const config: Config = {
  content: [],
  theme: {},
  plugins: [],
};

export default config;
